# 墨页验证记录

## 0.1.4 草稿恢复与阅读效率（2026-09-23）

- 新增草稿自动备份与恢复、阅读/编辑位置记忆、Ctrl+P 快速打开和代码块复制；不引入数据库或新的运行依赖。
- 18 项 TypeScript、7 项 Rust 单测通过，类型检查 0 错误、0 警告；Windows 发布构建成功。
- 从最终 NSIS 安装包解出的程序运行 14 组新功能检查：原生剪贴板与拒绝访问；重启、缩放及公式图表加载后的定位；编辑模式与光标恢复；链接锚点优先；多草稿强制退出恢复、稍后处理及冲突；取消/放弃与备份清理；索引刷新、深色窄窗口与 703 份 Markdown 文件目录；原文件删除、只读失败、备份 I/O 失败重试、损坏草稿隔离及缩短文档位置限制。检查脚本为 `scripts/check-productivity.mjs`。
- 13 组既有桌面检查、8 组 Mermaid/同步滚动检查、7 组缩放检查、4 组原生 PDF 检查均通过。PDF 长文 5 页、未保存文档 1 页，均为 A4、可提取文字、白色页边，无复制按钮文字。
- NSIS 安装包 **3,330,025 字节（3.33 MB）**，程序 **5,524,480 字节（5.52 MB）**；安装包比 0.1.3 增加 31,399 字节。程序与第三方许可有效载荷约 8.17 MB，不含 WebView2、卸载器及用户数据。
- 本机单次启动至首份文档显示 1,027 ms，约 1 MB 中文文档打开排版 632 ms；小文档时所有应用及 WebView2 进程私有提交内存之和约 263.3 MiB，工作集之和约 389.6 MiB（包含共享页重复计算）。沿用下方首版测量方法，不代表所有电脑。
- 原始报告位于 `artifacts/productivity-report.json`、`artifacts/native-report.json`、`artifacts/native-reading-fixes-report.json`、`artifacts/zoom-report.json`、`artifacts/pdf-report.json`。所有原生脚本使用独立 `INKDOWN_DATA_DIR`，不会读写正常使用的草稿。
- 草稿仅能恢复最后一次成功备份的内容；停止输入 2 秒、持续输入最长 10 秒触发备份，最后一个备份间隔内的输入可能未落盘。没有新增自动覆盖原文件或云同步功能。

## 0.1.3 文档缩放（2026-09-18）

- 文档缩放 50%–200%，步进 10%；工具栏、快捷键、Ctrl+滚轮、一键恢复及比例持久化。
- 浏览器与最终 NSIS 解包程序各通过 7 组缩放检查，包含上下限、编辑文字字号、放大后的同步滚动、切换标签、重载恢复及窄窗口布局。
- 13 项 TypeScript 测试通过，类型检查 0 错误、0 警告，Windows 发布构建成功。Rust 后端未修改。
- 在 200% 缩放下重跑 4 组原生 PDF 流程：长文仍为 5 页 A4，未保存文档为 1 页，文字可提取、页边为白色；取消及只读失败保留正常状态。
- NSIS 安装器 3,298,626 字节（3.30 MB），比 0.1.2 增加 988 字节。未新增运行依赖。
- 原始报告：`artifacts/zoom-report.json`、`artifacts/pdf-report.json`。新增检查脚本：`scripts/check-zoom.mjs`。

## 0.1.2 PDF 导出（2026-09-18）

- 新增 WebView2 原生 PDF 导出，菜单「更多操作 → 导出为 PDF」，快捷键 Ctrl+Shift+P。A4 纵向、15 mm 页边距、白底，导出当前内容及未保存编辑；源文档不变。
- 从最终 NSIS 安装器中解包测试：4 组 PDF 流程（长文、公式、图表、本地图片；深色与未保存编辑；取消；只读失败）通过。检查 5 页长文及 1 页未保存文档的 A4 尺寸、可提取文字与白色页边，并将全部 PDF 页渲染为 PNG 进行目视检查。
- 13 项既有桌面测试和 11 项 TypeScript 测试通过，类型检查 0 错误、0 警告；Rust 编译与发布构建通过。PDF 测试使用真实 WebView2 打印接口，仅注入保存对话框选择的路径。
- NSIS 安装器 3,297,638 字节（3.30 MB），相比 0.1.1 增加 11,255 字节。PDF 复用系统 WebView2，没有捆绑额外浏览器。
- 原始记录：`artifacts/pdf-report.json`、`artifacts/native-report.json`；检查脚本：`scripts/check-pdf.mjs` 与 `scripts/validate-pdf.py`（后者需要 pypdf、pypdfium2、Pillow）。
- 暂不提供纸张、横向、页眉页脚的配置；缺失图片或错误公式/图表保留相应的占位/错误内容。

## 0.1.1 更新（2026-09-17）

- 修复 Mermaid 框图文字消失：在根配置禁用 HTML 标签，使用原生 SVG 文字，仍保留严格模式和 SVG 清理。浅色、深色均验证节点与连线文字可见。
- 编辑区滚动驱动预览同步：依据源码行与渲染块插值，首尾对齐；预览可以独立滚动。切换标签、调整分栏、回到阅读模式均有回归测试。
- TypeScript 测试由 7 项增至 11 项；新增 `scripts/check-reading-fixes.mjs` 的 8 项浏览器/桌面检查，包括上下滚动与章节对应位置。
- 本次实际重跑：11 项 TypeScript、9 项既有浏览器交互、8 项浏览器修复回归、8 项桌面修复回归及 13 项既有桌面测试，共 49 项通过，运行时无错误；类型检查 0 错误、0 警告。Rust 源码未修改，原有 4 项 Rust 单测本次未重跑。
- 两组桌面测试均运行 `artifacts/installer-0.1.1/inkdown.exe`（从新 NSIS 包解压），既有桌面测试校验内置资源与本次构建一致，并验证断网渲染。
- 新安装包 **3,286,383 字节（3.29 MB）**，程序 **5,406,720 字节（5.41 MB）**。本机单次启动 1,477 ms，约 1 MB 文档打开排版 685 ms；测量方式与下方首版记录相同。
- 最新测试报告为 `artifacts/reading-fixes-report.json`、`artifacts/native-reading-fixes-report.json`、`artifacts/ui-report.json`、`artifacts/native-report.json`；首版下表保留历史测量值。

## 0.1.0 首版验证记录

日期：2026-09-17。环境：Windows 10 IoT 企业版 LTSC 10.0.19044 x64。最终桌面测试从 **NSIS 安装包解压出的程序**运行，确认使用 `http://tauri.localhost` 内置资源，并校验加载的前端资源名与当前 `dist/index.html` 一致。

## 交付体积与性能

| 项目 | 实测结果 |
| --- | --- |
| NSIS 安装包，包含第三方许可 | 3,273,258 字节（3.27 MB / 3.12 MiB） |
| 免安装可执行文件 | 5,374,464 字节（5.37 MB / 5.13 MiB） |
| 安装有效载荷：程序 + 许可文本 | 8,018,839 字节（8.02 MB）；不含安装时生成的卸载器、WebView2 和用户缓存 |
| 启动至首份文档标题显示 | 1,463 ms；独立全新 WebView 配置目录，包含自动化连接时间 |
| 约 1 MB 中文文档打开并排版 | 564 ms |
| 小文档阅读时私有提交内存 | 约 255.1 MiB，包含程序及所有 WebView2 子进程 |
| 小文档阅读时工作集之和 | 约 374.6 MiB，含共享页的重复计数；Rust 主进程约 25.4 MiB |
| 文件夹列表 | 700 份文档，首屏只渲染 150 个文件项，其余通过「显示更多」展开 |

启动与性能是本机单次端到端测量，不代表所有电脑。内存包含 WebView2 的浏览器、GPU、网络和渲染进程；不能只用小巧的 Rust 主进程来代表整个应用。详细原始数据在 `artifacts/native-report.json`；运行图表和打开大文档后内存会增加。

## 自动验证：33 项通过

- **7 项 TypeScript 测试**：原始 HTML/危险链接防护、表格/任务列表/高亮、大纲 ID、数学公式与代码边界、图片属性转义、大文档解析、Windows/中文文件引用。
- **4 项 Rust 测试**：UTF-8 BOM/CRLF 保存往返、外部修改拒绝覆盖、无效编码和删除冲突、图片目录边界。
- **9 项浏览器交互测试**：公式和图表、目录跳转、阅读搜索、编辑实时预览、跨标签内容和撤销、未保存取消/放弃、深色主题、125%/150% 等效可用视口布局。
- **13 项打包桌面测试**：启动参数、真实 BOM/CRLF 保存、冲突取消/明确覆盖、另存为、重复打开去重、只读文件保留编辑、无效 UTF-8 提示、断网重载后公式/图表/本地图片、错误渲染隔离与脚本防护、相对文档链接、单实例传递文件、关闭窗口取消后保留编辑、约 1 MB 文档、700 文件目录分页。
- `npm run check`：0 错误，0 警告；Vite 发布构建、Rust release 构建、NSIS 打包均成功。
- 最终桌面测试无 JavaScript 运行时错误；断网渲染不依赖开发服务器或 CDN。

## 验证范围与限制

- 自动桌面测试注入文件选择框的返回路径，实际读写、保存、冲突检查及 UI 状态均走正式程序。Windows 原生选择框中的鼠标操作尚未人工验证。
- 已检查生成的 NSIS 脚本中三种 Markdown 扩展名的注册及卸载逻辑，实际通过资源管理器默认应用设置双击打开仍需人工确认；启动参数和单实例转发已通过真实进程测试。
- 已解包校验并运行安装器中的程序，未执行系统安装/卸载，因此没有改动当前电脑的 Markdown 默认应用关联。完整安装目录占用（包括生成的卸载器）未实测，上表给出可核验的安装有效载荷。
- 已检查模拟 125%/150% 可用视口的界面布局；未更改系统显示缩放设置。原生拖放事件处理已实现，实际鼠标跨程序拖放尚未人工验证。
- 未签名；没有代码签名证书、崩溃恢复或自动更新。首次安装到缺少 WebView2 的电脑上需要联网补齐运行时。

## 重现

参见 `README.md`。原始自动化报告为 `artifacts/ui-report.json` 和 `artifacts/native-report.json`，截图包括 `reading-light.png`、`reading-dark.png`、`editing-light.png`、`native-reading.png`。

```powershell
# 测试安装器解包出来的可执行文件
$env:INKDOWN_TEST_BINARY = "$PWD\artifacts\installer-extracted\inkdown.exe"
node scripts/check-native.mjs
```
